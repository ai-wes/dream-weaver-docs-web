// Basic MDX components mapping for next-mdx-remote
// Extend this object with custom components as needed

const components = {
  // Example: override how <a> tags are rendered
  // a: (props) => <a {...props} style={{ color: 'skyblue' }} />,
}

export { components } 